class Balls:
    def __init__(self, x, y, dx, dy, speed, ball_color):
        self.x = x
        self.y = y
        self.dx = dx
        self.dy = dy
        self.speed = speed
        self.ball_color = ball_color

    def ball_spawn(self):


    def move_ball(self):


    def try_bounce(self):


    def ball_respawn(self):


    def new_ball_color(self, new_color):


    def collide_brick(self, isRound):